# Azure DevOps organization name (the value in dev.azure.com/<organization>).
ADO_ORGANIZATION=your-organization

# Read-only PAT with Code > Read scope. Never commit the real token.
ADO_PAT=your-azure-devops-personal-access-token
